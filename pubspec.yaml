name: poker_scheduler
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.22+22

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.3.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.2.0
  firebase_storage: ^12.1.0

  google_sign_in: ^6.1.6
  image_picker: ^1.0.7
  http: ^1.2.1
  url_launcher: ^6.2.5
  universal_html: ^2.2.4
  sign_in_with_apple: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true